{
  "name": "demo-playwright-vue-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "client-dev": "cd client && yarn dev",
    "client-format": "cd client && yarn format",
    "format": "yarn client-format"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "server",
    "client"
  ]
}